A request with no cookie should redirect to github auth login
After github auth login, should check membership of XYZ Organisation
Should retrieve name, email, github username from github
Should store name, email, username in cookie
Should display meaningful error if user is not member of XYZ organisation
Should display meaningful error if email is missing from github profile
Should display meaningful error if name is missing from github profile
Should redirect to original url following github auth
Should add name, email, username to request headers
Should reverse proxy through to target
Cookie domain should be *.x.y.com

Should accept config file containing multiple targets
Config file should enforce email and name are present for certain targets (headers then used for auth)
Config file should enable ip whitelist, bypassing github auth for non-enforced targets

Should accept configurable Github Org
